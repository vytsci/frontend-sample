/**
 * Some component A that is only used in one or few pages
 *
 * @type {{init: ComponentA.init}}
 */
ComponentA = {
  init: function () {
    console.log('Initialization of ComponentA script');
  },
};
