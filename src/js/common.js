/**
 * Common script that is used all over across application
 *
 * @type {{init: Common.init}}
 */
Common = {
  init: function () {
    console.log('Initialization of Common script');
  },
};

$(Common.init);
